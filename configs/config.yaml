# Default config for general modules

modules_enabled:
  stt_module: false
  nlp_module: false
  mem_module: false
  llm_module: false
  tts_module: true
  sys_module: false

logging:
  enabled: true
  log_level: "DEBUG"
  log_dir: "logs"
  enable_split_logs: true

debug:
  enabled: true
  debug_level: 3
