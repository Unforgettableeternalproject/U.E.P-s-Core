# UEP 核心框架使用指南

## 概述

UEP 核心框架是一個統一的、可擴展的系統架構，將原有的線性模組通信轉換為智能的網狀通信模式。框架整合了 Working Context、State Management、智能路由和會話管理等核心功能。

## 核心特性

### 🔄 統一狀態管理
- 整合 Working Context Manager、State Manager 和 Session Manager
- 全局狀態一致性
- 智能狀態轉換

### 🧠 智能路由決策
- 基於模組能力和依賴關係的動態路由
- 多種路由策略：智能路由、優先級路由、條件路由
- 上下文感知的決策引擎

### 🔌 可擴展架構
- 模組註冊和發現機制
- 統一的模組通信協議
- 插件式決策處理器

### 🕸️ 網狀通信模式
- 模組間可直接通信
- 基於需求的動態模組調用
- 支援複雜的執行流程

## 快速開始

### 1. 初始化框架

```python
from core.unified_controller import unified_controller

# 初始化整個系統
success = unified_controller.initialize()

if success:
    print("✅ 系統初始化成功")
    
    # 獲取系統狀態
    status = unified_controller.get_system_status()
    print(f"系統健康狀態: {status['system_health']}")
    print(f"已註冊模組: {status['enabled_modules']}")
else:
    print("❌ 系統初始化失敗")
```

### 2. 處理用戶輸入

```python
# 處理聊天輸入
chat_result = unified_controller.process_input(
    intent="chat",
    data={"text": "你好，今天天氣怎麼樣？"},
    strategy="smart"  # 使用智能路由策略
)

# 處理指令輸入
command_result = unified_controller.process_input(
    intent="command", 
    data={"text": "幫我建立一個新檔案"},
    strategy="smart"
)

print(f"聊天結果: {chat_result}")
print(f"指令結果: {command_result}")
```

### 3. 啟動語音識別

```python
# 定義語音結果處理函數
def handle_voice_result(result):
    if result.get("should_activate") and result.get("text"):
        print(f"🎤 語音識別: {result['text']}")
        
        # 自動處理語音輸入
        processing_result = unified_controller.process_input(
            intent="voice_recognition",
            data=result
        )
        print(f"處理結果: {processing_result}")

# 啟動實時語音識別
unified_controller.process_voice_input(callback=handle_voice_result)

# 停止語音識別
# unified_controller.stop_voice_input()
```

## 核心組件詳解

### CoreFramework 類

核心框架的主要類別，整合所有核心功能：

```python
from core.framework import core_framework

# 註冊模組
core_framework.register_module(
    module_id="custom_module",
    module_instance=your_module,
    capabilities=["custom_capability"],
    dependencies=["nlp"],
    priority=5
)

# 計算路由
route = core_framework.calculate_optimal_route(
    intent="custom_intent",
    context={"key": "value"},
    strategy_name="smart"
)

# 執行管線
result = core_framework.execute_pipeline(
    intent="chat",
    data={"text": "Hello"},
    execution_mode=ExecutionMode.SEQUENTIAL
)
```

### 路由策略

#### 智能路由策略 (SmartRouteStrategy)
基於模組能力、依賴關係和上下文進行智能路由：

```python
from core.strategies import smart_strategy

# 智能路由會自動：
# 1. 分析意圖需求
# 2. 查找具備相應能力的模組
# 3. 根據上下文優化執行順序
# 4. 考慮模組依賴關係
```

#### 優先級路由策略 (PriorityRouteStrategy)
基於模組優先級進行路由：

```python
from core.strategies import priority_strategy

# 優先級路由會：
# 1. 按模組優先級排序
# 2. 選擇與意圖相關的高優先級模組
# 3. 限制執行模組數量
```

#### 條件路由策略 (ConditionalRouteStrategy)
基於自定義條件進行路由：

```python
from core.strategies import conditional_strategy

# 添加自定義條件
def my_condition(intent, context, modules):
    return context.get("special_mode", False)

conditional_strategy.add_condition(
    "special_mode",
    my_condition,
    ["special_module", "llm"]
)
```

### Working Context 整合

框架完全整合了 Working Context 系統：

```python
from core.working_context import ContextType

# 添加上下文資料
context_id = core_framework.add_context_data(
    context_type=ContextType.SPEAKER_ACCUMULATION,
    data=speaker_embedding,
    metadata={"confidence": 0.85}
)

# 獲取上下文狀態
status = core_framework.get_context_status(context_id)

# 註冊決策處理器
core_framework.register_decision_handler(
    ContextType.SPEAKER_ACCUMULATION,
    your_decision_handler
)
```

## 開發和調試

### 使用調試工具

框架提供了完整的調試工具集，位於 `devtools/debug_functions.py`：

```python
from devtools.debug_functions import module_debugger

# 模組功能測試
module_debugger.stt_test_single()          # STT 單次測試
module_debugger.nlp_test("你好")            # NLP 測試
module_debugger.mem_fetch_test("記憶查詢")   # 記憶查詢測試
module_debugger.llm_test_chat("聊天測試")    # LLM 聊天測試
module_debugger.tts_test("語音合成")         # TTS 測試

# Working Context 調試
module_debugger.get_working_context_status()    # 獲取上下文狀態
module_debugger.test_speaker_context_workflow() # 語者上下文測試

# 語者管理
module_debugger.stt_speaker_list()             # 列出說話人
module_debugger.stt_speaker_rename("舊", "新")  # 重命名說話人
module_debugger.stt_speaker_delete("speaker")  # 刪除說話人
```

### 架構職責分離

新的架構清晰分離了不同職責：

#### 統一控制器 (`core/unified_controller.py`)
- ✅ 系統生命週期管理
- ✅ 模組註冊和協調
- ✅ 事件處理和狀態管理
- ✅ 實際的業務控制邏輯

#### 調試工具 (`devtools/debug_functions.py`)
- ✅ 模組功能測試
- ✅ 性能監控和統計
- ✅ 開發調試工具
- ✅ 系統診斷功能

#### 核心框架 (`core/framework.py`)
- ✅ 執行引擎和管道處理
- ✅ 路由決策和模組調度
- ✅ 數據轉換協調

### 舊控制器遷移

原有的 `core/controller.py` 將逐步棄用：

```python
# ❌ 舊方式 (將被棄用)
from core.controller import stt_test_single

# ✅ 新方式 (推薦)
from devtools.debug_functions import module_debugger
module_debugger.stt_test_single()

# 或者使用別名保持向後兼容
from devtools.debug_functions import stt_test_single
```

## 模組 Schema 重構計劃

### 統一數據格式

未來的模組重構將採用統一的數據 Schema：

```python
from pydantic import BaseModel, Field
from typing import Optional, Dict, Any

class UnifiedModuleData(BaseModel):
    """統一的模組數據格式"""
    # 核心數據
    text: Optional[str] = None
    intent: Optional[str] = None
    context: Optional[Dict[str, Any]] = None
    
    # 模組間傳遞的 metadata
    metadata: Dict[str, Any] = Field(default_factory=dict)
    
    # 執行控制
    execution_mode: Optional[str] = None
    priority: Optional[int] = None
    
    # 模組特定數據 (靈活擴展)
    module_data: Dict[str, Any] = Field(default_factory=dict)
```

### 重構優先級

1. **NLP 模組** - 統一意圖分類格式
2. **MEM 模組** - 統一記憶查詢和儲存格式
3. **LLM 模組** - 統一對話和指令處理格式
4. **TTS 模組** - 統一語音合成請求格式
5. **STT 模組** - 統一語音識別輸出格式

### 向後兼容

重構期間將保持向後兼容：
- 保留現有 Schema 作為 legacy 支援
- 新 Schema 作為主要介面
- DataTransformer 將逐步簡化

## 架構對比

### 舊架構 (線性)
```
STT → NLP → MEM/LLM → TTS → SYS
     ↑              ↑       ↑
  數據轉換      數據轉換  數據轉換
```

### 新架構 (網狀)
```
    ┌─────────────────────────────────┐
    │        Core Framework           │
    │  ┌─────────────────────────────┐ │
    │  │      Router Engine          │ │
    │  │   ┌─────────────────────┐   │ │
    │  │   │   Smart Routing     │   │ │
    │  │   │   Context Aware     │   │ │
    │  │   │   State Management  │   │ │
    │  │   └─────────────────────┘   │ │
    │  └─────────────────────────────┘ │
    └─────────────────────────────────┘
              │
         ┌────┴────┐
    STT ◄─► NLP ◄─► MEM
     ▲       ▲       ▲
     │       │       │
     ▼       ▼       ▼
    TTS ◄─► LLM ◄─► SYS
```

# 獲取當前狀態
current_state = core_framework.get_current_state()

# 設置狀態
core_framework.set_state(UEPState.WORK, {"task": "file_processing"})

# 處理狀態事件
core_framework.handle_state_event("command", {
    "status": "success",
    "result": "operation completed"
})
```

### 會話管理

工作流會話管理：

```python
# 創建工作流會話
session_id = core_framework.create_workflow_session(
    workflow_type="file_processing",
    command="處理文件",
    initial_data={"file_path": "/path/to/file"}
)

# 獲取會話
session = core_framework.get_session(session_id)

# 完成會話
core_framework.complete_session(session_id, {"result": "success"})
```

### 事件系統

靈活的事件驅動機制：

```python
# 註冊事件處理器
def my_event_handler(event_data):
    print(f"收到事件: {event_data}")

core_framework.register_event_handler("custom_event", my_event_handler)

# 觸發事件
core_framework._emit_event("custom_event", {"message": "Hello"})
```

## 模組開發指南

### 創建新模組

```python
from core.module_base import BaseModule

class YourModule(BaseModule):
    def initialize(self):
        # 模組初始化邏輯
        pass
    
    def handle(self, data: dict) -> dict:
        # 處理邏輯
        return {"status": "success", "result": "processed"}
    
    def shutdown(self):
        # 清理資源
        pass

# 註冊模組
def register():
    return YourModule()
```

### 註冊到框架

```python
# 在統一控制器中註冊
unified_controller.framework.register_module(
    module_id="your_module",
    module_instance=YourModule(),
    capabilities=["your_capability"],
    dependencies=["required_module"],
    priority=5
)
```

### 模組能力定義

定義模組的功能能力：

```python
YOUR_MODULE_CAPABILITIES = [
    "text_processing",
    "data_analysis", 
    "custom_feature"
]
```

## 最佳實踐

### 1. 模組設計原則
- **單一職責**：每個模組專注於特定功能
- **鬆散耦合**：模組間透過標準介面通信
- **高內聚**：相關功能組織在同一模組內

### 2. 路由策略選擇
- **智能路由**：適用於複雜場景，需要考慮多種因素
- **優先級路由**：適用於有明確優先級要求的場景
- **條件路由**：適用於需要自定義邏輯的特殊場景

### 3. 錯誤處理
```python
try:
    result = unified_controller.process_input(intent, data)
    if result.get("status") == "error":
        print(f"處理失敗: {result.get('message')}")
except Exception as e:
    print(f"系統異常: {e}")
```

### 4. 性能優化
- 使用適當的執行模式（順序、並行、條件）
- 定期清理過期的上下文和會話
- 監控模組狀態和系統健康

### 5. 測試和調試
```python
# 獲取詳細的系統狀態
status = unified_controller.get_system_status()
print(f"系統狀態: {status}")

# 檢查模組可用性
available_modules = core_framework.get_available_modules()
print(f"可用模組: {list(available_modules.keys())}")

# 查看執行上下文
execution_context = core_framework.execution_context
print(f"執行上下文: {execution_context}")
```

## 系統關閉

```python
# 正確關閉系統
unified_controller.shutdown()
```

## 範例：完整的語音助手流程

```python
from core.unified_controller import unified_controller

def main():
    # 1. 初始化系統
    if not unified_controller.initialize():
        print("❌ 系統初始化失敗")
        return
    
    print("✅ UEP 系統已啟動")
    
    # 2. 設置語音處理回調
    def process_voice(result):
        if result.get("should_activate"):
            text = result.get("text", "")
            print(f"🎤 語音輸入: {text}")
            
            # 自動處理語音
            response = unified_controller.process_input("chat", {"text": text})
            print(f"🤖 回應: {response.get('text', '處理完成')}")
    
    # 3. 啟動語音識別
    unified_controller.process_voice_input(callback=process_voice)
    
    try:
        print("🎙️ 語音助手已啟動，按 Ctrl+C 停止...")
        while True:
            time.sleep(1)
    except KeyboardInterrupt:
        print("\n⏹️ 正在停止...")
    finally:
        unified_controller.shutdown()
        print("✅ 系統已關閉")

if __name__ == "__main__":
    main()
```