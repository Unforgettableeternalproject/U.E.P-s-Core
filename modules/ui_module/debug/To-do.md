# 待辦事項 (除錯介面)

1. 更好的**終端整合**，模組的測試會實時演示 (就像debug_api中做的那些終端印出訊息，不過顯示在UI中)

2. ~~Core相關資訊蒐集 (類似Context、State等等)~~ 除錯模式下不會有這些東西

3. **單元測試**嵌入 (於unit_test中)

4. 提高大多數Log等級，不然日誌會太多 (KEY為最小、ELABORATIVE為最大)

5. **效能指標**還沒處理

6. **整合測試的相關部分**我都還沒有動

7. **記憶體**監控的部分也還是有問題

>簡單來說，我們現在還是先以原有的除錯介面為主